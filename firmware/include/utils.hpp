#pragma once

#include <stdint.h>

/**
 * @brief   Logs the statistics of the current task.
 * @param   tag The tag to use for logging.
 * @param   taskName The name of the task.
 */
void logTaskStats(const char* tag, const char* taskName);
